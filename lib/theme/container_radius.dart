import 'package:flutter/widgets.dart';

class ContainerRadius {
  static BorderRadiusGeometry primary = BorderRadius.circular(10);
  static BorderRadiusGeometry secondry = BorderRadius.circular(20);
}
